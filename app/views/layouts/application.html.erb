<!DOCTYPE html>
<html>
  <head>
    <title><%= @page_title %><%= " • " if @page_title.present? %>Given to Tri’s Playlist Generator</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <% if ENV['FONT_AWESOME_KEY'].present?%>
      <script src="https://kit.fontawesome.com/<%= ENV['FONT_AWESOME_KEY'] %>.js" crossorigin="anonymous"></script>
    <% end %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <%= render partial: "shared/navbar" %>
    <section class="section">
      <% flash.each do |key, value| %>
        <div class="notification is-light <%= notification_class(key) %>" data-controller="notification">
          <button class="delete" data-action="click->notification#close" data-notification-target="closeButton"></button>
          <%= value %>
        </div>
      <% end %>
      <%= yield %>
    </section>
  </body>
</html>
