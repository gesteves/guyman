<tr id="<%= dom_id(track) %>" data-turbo-frame="<%= dom_id(track) %>">
  <td>
    <%= link_to track.title, track.spotify_url, target: '_blank' %> &ndash; <%= track.artist %>
  </td>
  <td>
    <div class="field is-grouped is-grouped-right">
      <%= button_to track_path(track), method: :delete, data: { turbo_confirm: 'Are you sure you want to delete this track? This can’t be undone.' }, class: "button is-danger" do %>
        <span class="icon">
          <i class="fa-solid fa-trash"></i>
        </span>
      <% end %>
    </div>
  </td>
</tr>
