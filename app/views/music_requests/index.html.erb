<section class="hero is-small">
  <div class="hero-body">
    <h1 class="title">Requests</h1>
    <p class="subtitle">This is the history of music requests you’ve made.</p>
    <hr>
  </div>
</section>

<section class="container is-max-desktop">
  <%= turbo_frame_tag "music_requests" do %>
    <% @music_requests.each do |request| %>
      <%= render partial: 'music_requests/card', locals: { music_request: request } %>
    <% end %>
  <% end %>
  <%= paginate @music_requests, outer_window: 1, window: 1 %>
</section>

<%= turbo_stream_from "music_requests:user:#{current_user.id}" %>
